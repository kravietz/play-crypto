# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /StringEnvelope                      controllers.Application.index2()
GET     /PlayCrypto                          controllers.Application.index()
GET     /                                    controllers.Application.index()
